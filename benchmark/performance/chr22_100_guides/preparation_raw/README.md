# Raw 100-Guide Preparation Validation Outputs

Raw Columba k=0 SAM files and CRISPRapido exact-match PAF validation outputs generated by `../prepare_chr22_100_guides.py` are written here and intentionally excluded from Git.

The compact validation result is retained in `../validation_summary.tsv`.
