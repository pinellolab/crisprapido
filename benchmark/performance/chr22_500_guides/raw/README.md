Raw benchmark outputs are generated by the Slurm and local batch scripts.

Timestamped run directories under this path are intentionally excluded from Git.
They contain PAF, SAM, stderr, metrics, and Slurm logs that can be regenerated
from the tracked guide panel, batch manifests, and scripts.
