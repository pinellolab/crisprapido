Guide-panel exact-match validation outputs are generated by `prepare_guides.py`.

This directory is intentionally excluded from Git because it contains per-guide
SAM, PAF, log, stdout, and stderr files. The compact validation summary is kept
at the package root.
